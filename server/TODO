IMPORTANT: make thiswebsite.com == http://thiswebsite.com!!
	-build recommendations for a user AFTER they are ADDED!
	-Make sure user info is actually updated TEST WITH PHIL
	-Generate a new unique id for a song every time it is posted to a group

SECONDARY:
	-modify add_song_to_source -> add songs to one source to reduce the number of db lookups.
	-add timeout to getRSS/scraping requests 
	-check the removing things -- make sure it removes user from USERS and user from any of the sources the user has.
